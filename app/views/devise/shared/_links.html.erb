<%- if devise_mapping.omniauthable? %>
  <div class="text-lg px-5 py-2.5">
    or
  </div>
  <%- resource_class.omniauth_providers.each do |provider| %>
    <%= button_to omniauth_authorize_path(resource_name, provider),
    data: {
      turbo: false,
    },
    class:
    "text-white bg-blue-600 hover:bg-blue-700 font-medium rounded-lg text-sm px-5 py-2.5 text-center" do %>
      <div class="flex items-center gap-2">
        <%= inline_svg_tag "users/github.svg", class: "w-6 h-6 mr-2" %>
        Sign in with
        <%= OmniAuth::Utils.camelize(provider) %>
      </div>
    <% end %>
    <br/>
  <% end %>
<% end %>

<%- if controller_name != 'sessions' %>
  <%= link_to "Log in",
  new_session_path(resource_name),
  class: "inline-block font-semibold underline hover:text-blue-700" %><br/>
<% end %>

<%- if devise_mapping.registerable? && controller_name != 'registrations' %>
  <%= link_to "Sign up",
  new_registration_path(resource_name),
  class: "inline-block font-semibold underline hover:text-blue-700" %><br/>
<% end %>

<%- if devise_mapping.recoverable? && controller_name != 'passwords' && controller_name != 'registrations' %>
  <%= link_to "Forgot your password?",
  new_password_path(resource_name),
  class: "inline-block font-semibold underline hover:text-blue-700" %><br/>
<% end %>

<%- if devise_mapping.confirmable? && controller_name != 'confirmations' %>
  <%= link_to "Didn't receive confirmation instructions?",
  new_confirmation_path(resource_name),
  class: "inline-block font-semibold underline hover:text-blue-700" %><br/>
<% end %>

<%- if devise_mapping.lockable? && resource_class.unlock_strategy_enabled?(:email) && controller_name != 'unlocks' %>
  <%= link_to "Didn't receive unlock instructions?",
  new_unlock_path(resource_name),
  class: "inline-block font-semibold underline hover:text-blue-700" %><br/>
<% end %>
